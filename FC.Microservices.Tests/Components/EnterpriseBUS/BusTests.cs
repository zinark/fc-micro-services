namespace FCMicroservices.Tests.Components.BUS;

[TestClass]
public class BusTests
{
    [TestMethod]
    public void foo()
    {
        //var handler = typeof(RunCustomerAdapterHandler);
        //var t = handler.BaseType;
        //System.Console.WriteLine(t.FullName);
        //var args = t.GetGenericArguments();

        //if (args.Length != 2) throw new ApiException("Handler TMessage ve TReply icermiyor! Handler kullanildigindan emin olun!");

        //var msg = args[0];
        //var reply = args[1];

        //foreach (var a in args)
        //{
        //    System.Console.WriteLine(a.FullName);
        //}
    }
}